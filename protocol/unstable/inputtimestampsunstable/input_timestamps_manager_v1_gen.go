// Code generated by wayland-scanner. DO NOT EDIT.

package inputtimestampsunstable

import (
	"github.com/xogas/wayland"
	"github.com/xogas/wayland/wire"
)

const InterfaceInputTimestampsManagerV1 = "zwp_input_timestamps_manager_v1"
const VersionInputTimestampsManagerV1 = 1

const (
	InputTimestampsManagerV1RequestDestroy               uint16 = 0
	InputTimestampsManagerV1RequestGetKeyboardTimestamps uint16 = 1
	InputTimestampsManagerV1RequestGetPointerTimestamps  uint16 = 2
	InputTimestampsManagerV1RequestGetTouchTimestamps    uint16 = 3
)

// InputTimestampsManagerV1DestroyRequest destroy the input timestamps manager object.
//
// Informs the server that the client will no longer be using this
// protocol object. Existing objects created by this object are not
// affected.
type InputTimestampsManagerV1DestroyRequest struct {
}

func (r *InputTimestampsManagerV1DestroyRequest) Opcode() uint16 {
	return InputTimestampsManagerV1RequestDestroy
}

func (r *InputTimestampsManagerV1DestroyRequest) Marshal(w *wire.Writer) error {
	return nil
}

func (r *InputTimestampsManagerV1DestroyRequest) Since() uint32 { return 1 }

// InputTimestampsManagerV1GetKeyboardTimestampsRequest subscribe to high-resolution keyboard timestamp events.
//
// Creates a new input timestamps object that represents a subscription
// to high-resolution timestamp events for all wl_keyboard events that
// carry a timestamp.
//
// If the associated wl_keyboard object is invalidated, either through
// client action (e.g. release) or server-side changes, the input
// timestamps object becomes inert and the client should destroy it
// by calling zwp_input_timestamps_v1.destroy.
type InputTimestampsManagerV1GetKeyboardTimestampsRequest struct {
	ID wire.NewID
	// Keyboard the wl_keyboard object for which to get timestamp events.
	Keyboard wire.ObjectID
}

func (r *InputTimestampsManagerV1GetKeyboardTimestampsRequest) Opcode() uint16 {
	return InputTimestampsManagerV1RequestGetKeyboardTimestamps
}

func (r *InputTimestampsManagerV1GetKeyboardTimestampsRequest) Marshal(w *wire.Writer) error {
	if err := w.NewID(r.ID); err != nil {
		return err
	}
	if err := w.Object(r.Keyboard); err != nil {
		return err
	}
	return nil
}

func (r *InputTimestampsManagerV1GetKeyboardTimestampsRequest) Since() uint32 { return 1 }

// InputTimestampsManagerV1GetPointerTimestampsRequest subscribe to high-resolution pointer timestamp events.
//
// Creates a new input timestamps object that represents a subscription
// to high-resolution timestamp events for all wl_pointer events that
// carry a timestamp.
//
// If the associated wl_pointer object is invalidated, either through
// client action (e.g. release) or server-side changes, the input
// timestamps object becomes inert and the client should destroy it
// by calling zwp_input_timestamps_v1.destroy.
type InputTimestampsManagerV1GetPointerTimestampsRequest struct {
	ID wire.NewID
	// Pointer the wl_pointer object for which to get timestamp events.
	Pointer wire.ObjectID
}

func (r *InputTimestampsManagerV1GetPointerTimestampsRequest) Opcode() uint16 {
	return InputTimestampsManagerV1RequestGetPointerTimestamps
}

func (r *InputTimestampsManagerV1GetPointerTimestampsRequest) Marshal(w *wire.Writer) error {
	if err := w.NewID(r.ID); err != nil {
		return err
	}
	if err := w.Object(r.Pointer); err != nil {
		return err
	}
	return nil
}

func (r *InputTimestampsManagerV1GetPointerTimestampsRequest) Since() uint32 { return 1 }

// InputTimestampsManagerV1GetTouchTimestampsRequest subscribe to high-resolution touch timestamp events.
//
// Creates a new input timestamps object that represents a subscription
// to high-resolution timestamp events for all wl_touch events that
// carry a timestamp.
//
// If the associated wl_touch object becomes invalid, either through
// client action (e.g. release) or server-side changes, the input
// timestamps object becomes inert and the client should destroy it
// by calling zwp_input_timestamps_v1.destroy.
type InputTimestampsManagerV1GetTouchTimestampsRequest struct {
	ID wire.NewID
	// Touch the wl_touch object for which to get timestamp events.
	Touch wire.ObjectID
}

func (r *InputTimestampsManagerV1GetTouchTimestampsRequest) Opcode() uint16 {
	return InputTimestampsManagerV1RequestGetTouchTimestamps
}

func (r *InputTimestampsManagerV1GetTouchTimestampsRequest) Marshal(w *wire.Writer) error {
	if err := w.NewID(r.ID); err != nil {
		return err
	}
	if err := w.Object(r.Touch); err != nil {
		return err
	}
	return nil
}

func (r *InputTimestampsManagerV1GetTouchTimestampsRequest) Since() uint32 { return 1 }

// InputTimestampsManagerV1 context object for high-resolution input timestamps.
//
// A global interface used for requesting high-resolution timestamps
// for input events.
type InputTimestampsManagerV1 struct {
	proxy *wayland.Proxy
}

// NewInputTimestampsManagerV1 wraps p in a InputTimestampsManagerV1 proxy.
func NewInputTimestampsManagerV1(p *wayland.Proxy) *InputTimestampsManagerV1 {
	return &InputTimestampsManagerV1{proxy: p}
}

// Proxy returns the underlying Wayland proxy.
func (o *InputTimestampsManagerV1) Proxy() *wayland.Proxy {
	return o.proxy
}

// Destroy destroy the input timestamps manager object.
//
// Informs the server that the client will no longer be using this
// protocol object. Existing objects created by this object are not
// affected.
func (o *InputTimestampsManagerV1) Destroy() error {
	if o.proxy.Deleted() {
		return nil
	}
	if err := o.proxy.SendRequest(InputTimestampsManagerV1RequestDestroy, &InputTimestampsManagerV1DestroyRequest{}); err != nil {
		return err
	}
	o.proxy.Conn().UnregisterProxy(o.proxy.ID())
	return nil
}

// GetKeyboardTimestamps subscribe to high-resolution keyboard timestamp events.
//
// Creates a new input timestamps object that represents a subscription
// to high-resolution timestamp events for all wl_keyboard events that
// carry a timestamp.
//
// If the associated wl_keyboard object is invalidated, either through
// client action (e.g. release) or server-side changes, the input
// timestamps object becomes inert and the client should destroy it
// by calling zwp_input_timestamps_v1.destroy.
func (o *InputTimestampsManagerV1) GetKeyboardTimestamps(keyboard wire.ObjectID) (*InputTimestampsV1, error) {
	conn := o.proxy.Conn()
	p := wayland.NewProxy(conn)
	p.SetVersion(o.proxy.Version())

	wrapped := NewInputTimestampsV1(p)
	conn.RegisterProxy(p)
	err := conn.SendRequest(o.proxy.ID(), InputTimestampsManagerV1RequestGetKeyboardTimestamps, &InputTimestampsManagerV1GetKeyboardTimestampsRequest{
		ID:       wire.NewID(p.ID()),
		Keyboard: keyboard,
	})
	if err != nil {
		conn.UnregisterProxy(p.ID())
		return nil, err
	}
	return wrapped, nil
}

// GetPointerTimestamps subscribe to high-resolution pointer timestamp events.
//
// Creates a new input timestamps object that represents a subscription
// to high-resolution timestamp events for all wl_pointer events that
// carry a timestamp.
//
// If the associated wl_pointer object is invalidated, either through
// client action (e.g. release) or server-side changes, the input
// timestamps object becomes inert and the client should destroy it
// by calling zwp_input_timestamps_v1.destroy.
func (o *InputTimestampsManagerV1) GetPointerTimestamps(pointer wire.ObjectID) (*InputTimestampsV1, error) {
	conn := o.proxy.Conn()
	p := wayland.NewProxy(conn)
	p.SetVersion(o.proxy.Version())

	wrapped := NewInputTimestampsV1(p)
	conn.RegisterProxy(p)
	err := conn.SendRequest(o.proxy.ID(), InputTimestampsManagerV1RequestGetPointerTimestamps, &InputTimestampsManagerV1GetPointerTimestampsRequest{
		ID:      wire.NewID(p.ID()),
		Pointer: pointer,
	})
	if err != nil {
		conn.UnregisterProxy(p.ID())
		return nil, err
	}
	return wrapped, nil
}

// GetTouchTimestamps subscribe to high-resolution touch timestamp events.
//
// Creates a new input timestamps object that represents a subscription
// to high-resolution timestamp events for all wl_touch events that
// carry a timestamp.
//
// If the associated wl_touch object becomes invalid, either through
// client action (e.g. release) or server-side changes, the input
// timestamps object becomes inert and the client should destroy it
// by calling zwp_input_timestamps_v1.destroy.
func (o *InputTimestampsManagerV1) GetTouchTimestamps(touch wire.ObjectID) (*InputTimestampsV1, error) {
	conn := o.proxy.Conn()
	p := wayland.NewProxy(conn)
	p.SetVersion(o.proxy.Version())

	wrapped := NewInputTimestampsV1(p)
	conn.RegisterProxy(p)
	err := conn.SendRequest(o.proxy.ID(), InputTimestampsManagerV1RequestGetTouchTimestamps, &InputTimestampsManagerV1GetTouchTimestampsRequest{
		ID:    wire.NewID(p.ID()),
		Touch: touch,
	})
	if err != nil {
		conn.UnregisterProxy(p.ID())
		return nil, err
	}
	return wrapped, nil
}

// BindInputTimestampsManagerV1 binds the zwp_input_timestamps_manager_v1 global advertised by the server.
// version is the version requested by the application; it must be within
// [1, VersionInputTimestampsManagerV1] or ErrVersionMismatch is returned. Servers newer
// than this library may advertise a higher version: clamp the advertised
// version with the builtin min, e.g. BindInputTimestampsManagerV1(reg, name, min(g.Version,
// VersionInputTimestampsManagerV1)), to bind at the highest mutually supported version.
func BindInputTimestampsManagerV1(b wayland.Binder, name uint32, version uint32) (*InputTimestampsManagerV1, error) {
	if version < 1 || version > VersionInputTimestampsManagerV1 {
		return nil, wayland.ErrVersionMismatch
	}
	p, err := b.Bind(name, InterfaceInputTimestampsManagerV1, version)
	if err != nil {
		return nil, err
	}
	x := NewInputTimestampsManagerV1(p)
	x.proxy.SetVersion(version)
	return x, nil
}
