// Code generated by wayland-scanner. DO NOT EDIT.

package xdgshellunstablev6

import (
	"github.com/xogas/wayland"
	"github.com/xogas/wayland/wire"
)

const InterfaceShellV6 = "zxdg_shell_v6"
const VersionShellV6 = 1

const (
	ShellV6RequestDestroy          uint16 = 0
	ShellV6RequestCreatePositioner uint16 = 1
	ShellV6RequestGetXdgSurface    uint16 = 2
	ShellV6RequestPong             uint16 = 3
)

const (
	ShellV6EventPing uint16 = 0
)

// shellv6EventFDCounts maps every event opcode of this interface
// to the number of fds it carries (0 for events without fds). Dispatch uses it
// to drain fds and to reject unknown opcodes as stream violations.
var shellv6EventFDCounts = map[uint16]int{
	0: 0,
}

func init() {
	wayland.RegisterInterfaceFDCounts(InterfaceShellV6, shellv6EventFDCounts)
}

type ShellV6Error uint32

const (
	// ShellV6ErrorRole given wl_surface has another role.
	ShellV6ErrorRole ShellV6Error = 0

	// ShellV6ErrorDefunctSurfaces xdg_shell was destroyed before children.
	ShellV6ErrorDefunctSurfaces ShellV6Error = 1

	// ShellV6ErrorNotTheTopmostPopup the client tried to map or destroy a non-topmost popup.
	ShellV6ErrorNotTheTopmostPopup ShellV6Error = 2

	// ShellV6ErrorInvalidPopupParent the client specified an invalid popup parent surface.
	ShellV6ErrorInvalidPopupParent ShellV6Error = 3

	// ShellV6ErrorInvalidSurfaceState the client provided an invalid surface state.
	ShellV6ErrorInvalidSurfaceState ShellV6Error = 4

	// ShellV6ErrorInvalidPositioner the client provided an invalid positioner.
	ShellV6ErrorInvalidPositioner ShellV6Error = 5
)

// ShellV6DestroyRequest destroy xdg_shell.
//
// Destroy this xdg_shell object.
//
// Destroying a bound xdg_shell object while there are surfaces
// still alive created by this xdg_shell object instance is illegal
// and will result in a protocol error.
type ShellV6DestroyRequest struct {
}

func (r *ShellV6DestroyRequest) Opcode() uint16 { return ShellV6RequestDestroy }

func (r *ShellV6DestroyRequest) Marshal(w *wire.Writer) error {
	return nil
}

func (r *ShellV6DestroyRequest) Since() uint32 { return 1 }

// ShellV6CreatePositionerRequest create a positioner object.
//
// Create a positioner object. A positioner object is used to position
// surfaces relative to some parent surface. See the interface description
// and xdg_surface.get_popup for details.
type ShellV6CreatePositionerRequest struct {
	ID wire.NewID
}

func (r *ShellV6CreatePositionerRequest) Opcode() uint16 { return ShellV6RequestCreatePositioner }

func (r *ShellV6CreatePositionerRequest) Marshal(w *wire.Writer) error {
	if err := w.NewID(r.ID); err != nil {
		return err
	}
	return nil
}

func (r *ShellV6CreatePositionerRequest) Since() uint32 { return 1 }

// ShellV6GetXdgSurfaceRequest create a shell surface from a surface.
//
// This creates an xdg_surface for the given surface. While xdg_surface
// itself is not a role, the corresponding surface may only be assigned
// a role extending xdg_surface, such as xdg_toplevel or xdg_popup.
//
// This creates an xdg_surface for the given surface. An xdg_surface is
// used as basis to define a role to a given surface, such as xdg_toplevel
// or xdg_popup. It also manages functionality shared between xdg_surface
// based surface roles.
//
// See the documentation of xdg_surface for more details about what an
// xdg_surface is and how it is used.
type ShellV6GetXdgSurfaceRequest struct {
	ID      wire.NewID
	Surface wire.ObjectID
}

func (r *ShellV6GetXdgSurfaceRequest) Opcode() uint16 { return ShellV6RequestGetXdgSurface }

func (r *ShellV6GetXdgSurfaceRequest) Marshal(w *wire.Writer) error {
	if err := w.NewID(r.ID); err != nil {
		return err
	}
	if err := w.Object(r.Surface); err != nil {
		return err
	}
	return nil
}

func (r *ShellV6GetXdgSurfaceRequest) Since() uint32 { return 1 }

// ShellV6PongRequest respond to a ping event.
//
// A client must respond to a ping event with a pong request or
// the client may be deemed unresponsive. See xdg_shell.ping.
type ShellV6PongRequest struct {
	// Serial serial of the ping event.
	Serial uint32
}

func (r *ShellV6PongRequest) Opcode() uint16 { return ShellV6RequestPong }

func (r *ShellV6PongRequest) Marshal(w *wire.Writer) error {
	if err := w.Uint32(r.Serial); err != nil {
		return err
	}
	return nil
}

func (r *ShellV6PongRequest) Since() uint32 { return 1 }

// ShellV6PingEvent check if the client is alive.
//
// The ping event asks the client if it's still alive. Pass the
// serial specified in the event back to the compositor by sending
// a "pong" request back with the specified serial. See xdg_shell.ping.
//
// Compositors can use this to determine if the client is still
// alive. It's unspecified what will happen if the client doesn't
// respond to the ping request, or in what timeframe. Clients should
// try to respond in a reasonable amount of time.
//
// A compositor is free to ping in any way it wants, but a client must
// always respond to any xdg_shell object it created.
type ShellV6PingEvent struct {
	// Serial pass this to the pong request.
	Serial uint32
}

func (e *ShellV6PingEvent) Opcode() uint16 { return ShellV6EventPing }

func (e *ShellV6PingEvent) Unmarshal(r *wire.Reader) error {
	serial, err := r.Uint32()
	if err != nil {
		return err
	}
	e.Serial = serial
	return nil
}

func (e *ShellV6PingEvent) Since() uint32 { return 1 }

// ShellV6PingFunc is a callback for Ping events.
type ShellV6PingFunc func(ev ShellV6PingEvent)

// ShellV6 create desktop-style surfaces.
//
// xdg_shell allows clients to turn a wl_surface into a "real window"
// which can be dragged, resized, stacked, and moved around by the
// user. Everything about this interface is suited towards traditional
// desktop environments.
type ShellV6 struct {
	proxy *wayland.Proxy
}

// NewShellV6 wraps p in a ShellV6 proxy.
func NewShellV6(p *wayland.Proxy) *ShellV6 {
	p.SetEventFDCounts(shellv6EventFDCounts)
	return &ShellV6{proxy: p}
}

// Proxy returns the underlying Wayland proxy.
func (o *ShellV6) Proxy() *wayland.Proxy {
	return o.proxy
}

// OnPing registers fn to receive Ping events.
func (o *ShellV6) OnPing(fn ShellV6PingFunc) {
	o.proxy.RegisterEvent(ShellV6EventPing, func(r *wire.Reader) {
		var ev ShellV6PingEvent

		if err := ev.Unmarshal(r); err != nil {
			o.proxy.Conn().FailEvent("Ping", err)
			return
		}

		fn(ev)
	})
}

// Destroy destroy xdg_shell.
//
// Destroy this xdg_shell object.
//
// Destroying a bound xdg_shell object while there are surfaces
// still alive created by this xdg_shell object instance is illegal
// and will result in a protocol error.
func (o *ShellV6) Destroy() error {
	if o.proxy.Deleted() {
		return nil
	}
	if err := o.proxy.SendRequest(ShellV6RequestDestroy, &ShellV6DestroyRequest{}); err != nil {
		return err
	}
	o.proxy.Conn().UnregisterProxy(o.proxy.ID())
	return nil
}

// CreatePositioner create a positioner object.
//
// Create a positioner object. A positioner object is used to position
// surfaces relative to some parent surface. See the interface description
// and xdg_surface.get_popup for details.
func (o *ShellV6) CreatePositioner() (*PositionerV6, error) {
	conn := o.proxy.Conn()
	p := wayland.NewProxy(conn)
	p.SetVersion(o.proxy.Version())

	wrapped := NewPositionerV6(p)
	conn.RegisterProxy(p)
	err := conn.SendRequest(o.proxy.ID(), ShellV6RequestCreatePositioner, &ShellV6CreatePositionerRequest{
		ID: wire.NewID(p.ID()),
	})
	if err != nil {
		conn.UnregisterProxy(p.ID())
		return nil, err
	}
	return wrapped, nil
}

// GetXdgSurface create a shell surface from a surface.
//
// This creates an xdg_surface for the given surface. While xdg_surface
// itself is not a role, the corresponding surface may only be assigned
// a role extending xdg_surface, such as xdg_toplevel or xdg_popup.
//
// This creates an xdg_surface for the given surface. An xdg_surface is
// used as basis to define a role to a given surface, such as xdg_toplevel
// or xdg_popup. It also manages functionality shared between xdg_surface
// based surface roles.
//
// See the documentation of xdg_surface for more details about what an
// xdg_surface is and how it is used.
func (o *ShellV6) GetXdgSurface(surface wire.ObjectID) (*SurfaceV6, error) {
	conn := o.proxy.Conn()
	p := wayland.NewProxy(conn)
	p.SetVersion(o.proxy.Version())

	wrapped := NewSurfaceV6(p)
	conn.RegisterProxy(p)
	err := conn.SendRequest(o.proxy.ID(), ShellV6RequestGetXdgSurface, &ShellV6GetXdgSurfaceRequest{
		ID:      wire.NewID(p.ID()),
		Surface: surface,
	})
	if err != nil {
		conn.UnregisterProxy(p.ID())
		return nil, err
	}
	return wrapped, nil
}

// Pong respond to a ping event.
//
// A client must respond to a ping event with a pong request or
// the client may be deemed unresponsive. See xdg_shell.ping.
func (o *ShellV6) Pong(serial uint32) error {
	return o.proxy.SendRequest(ShellV6RequestPong, &ShellV6PongRequest{
		Serial: serial,
	})
}

// BindShellV6 binds the zxdg_shell_v6 global advertised by the server.
// version is the version requested by the application; it must be within
// [1, VersionShellV6] or ErrVersionMismatch is returned. Servers newer
// than this library may advertise a higher version: clamp the advertised
// version with the builtin min, e.g. BindShellV6(reg, name, min(g.Version,
// VersionShellV6)), to bind at the highest mutually supported version.
func BindShellV6(b wayland.Binder, name uint32, version uint32) (*ShellV6, error) {
	if version < 1 || version > VersionShellV6 {
		return nil, wayland.ErrVersionMismatch
	}
	p, err := b.Bind(name, InterfaceShellV6, version)
	if err != nil {
		return nil, err
	}
	x := NewShellV6(p)
	x.proxy.SetVersion(version)
	return x, nil
}
